<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.elijah.ukeme.doctorsappointmentapplication">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/hospital_bed"
        android:label="@string/app_name"
        android:roundIcon="@drawable/hospital_bed"
        android:supportsRtl="true"
        android:theme="@style/Theme.DoctorsAppointmentApplication"
        android:usesCleartextTraffic="true">
        <activity android:name=".activities.HomeActivity"></activity>
        <activity android:name=".activities.TokenActivity" />
        <activity android:name=".activities.AppointmentInformationActivity" />
        <activity android:name=".activities.UpdateActivity" />
        <activity android:name=".activities.ProfileActivity" />
        <activity android:name=".activities.BookAppointmentActivity" />
        <activity android:name=".activities.PatientRegistrationActivity" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>

</manifest>